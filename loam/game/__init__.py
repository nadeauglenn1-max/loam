"""The explore client — a pygame window over the live world.

Optional: install with the ``[game]`` extra (``pip install -e ".[game]"``). The
pure spatial layout lives in ``layout.py`` (no pygame, so it's testable
headless); the render loop lives in ``explore.py``.
"""
