# Loam
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
.coverage
# playthroughs: disposable local state, forked from a base — not committed
# (bases live in worlds/ and ARE committed — they're shareable content)
runtime/
!runtime/.gitkeep
